# 무시하고 싶은 폴더, 이름 넣으면 git이 무시하고 repesitory에 올리지 않습니다.
# 공유하고 싶은 프로젝트의 최상위 디렉터리에 위치해야 한다.

# 파일명은 걍 파일명

# 디렉터리는 / 를 뒤에 적는다.
.venv/

# * 이 모든 문자를 의미하는 와일드카드로 가능하다.
*/__pycache__/

# txt 확장자를 가진 모든 파일 무시
*.txt

# ! 를 파일명 앞에 쓰면 앞의 규칙에서 제외라는 뜻이다.
!test.txt